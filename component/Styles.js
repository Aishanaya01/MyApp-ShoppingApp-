import { StyleSheet, Dimensions } from "react-native";

export default StyleSheet.create({
    
    dotView: {
        marginBottom: 20,
        flexDirection: "row",
        justifyContent: "center",
      },
      dot: {
        height: 10,
        width: 10,
        backgroundColor: "#fff",
        margin: 8,
        borderRadius: 5,
      }
    }
      );

